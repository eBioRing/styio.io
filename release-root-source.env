# No release-root bundle is pinned while public binary releases are withdrawn.
# Pages deployments should publish only the source-controlled static site until
# a rebuilt release-root is audited and approved.
RELEASE_BUNDLE_REPO=
RELEASE_BUNDLE_TAG=
RELEASE_BUNDLE_ASSET=
